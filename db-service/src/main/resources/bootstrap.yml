server:
  port: ${ENROLLMENT_DB_SERVICE_PORT}
spring:
  application:
    name: ${ENROLLMENT_DB_SERVICE_NAME}

  r2dbc:
    url: ${ENROLLMENT_DB_URI}
    username: ${ENROLLMENT_DB_USER_NAME}
    password: ${ENROLLMENT_DB_PASSWORD}
