server:
  port: 1234
spring:
  application:
    name: abha-enrollment-test-app

logging:
  level:
    root: INFO
  pattern:
    level: "[%X{user_name}] [%X{traceId}/%X{spanId}] %-5p [%t] %C{2} - %m%n"
